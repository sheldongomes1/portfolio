# Assumptions and interpretation log

PRD: "Skill Gate + Portfolio Brief Gate for Google Workspace", v2 (2026-09-24).

Every assumption made to proceed, and every place where the PRD or a source file was ambiguous
and the stricter reading was chosen (PRD §0.7). **Flag** marks items that need an owner
decision.

| # | Milestone | Assumption or interpretation | Why | Stricter? | Flag |
|---|---|---|---|---|---|
| A1 | M1 | Work is staged in the `portfolio` repo under `staging/`: `staging/skill-gate/` becomes the new `skill-gate` repo, and `staging/portfolio-brief-gate/workspace/` becomes `workspace/` in `portfolio-brief-gate`. No new public repository was created, and nothing was pushed to `portfolio-brief-gate`. | The build session can push only to `portfolio` on one branch. Creating a public repo is outward-facing and needs the owner's go-ahead. The staged folders mirror the target paths, so moving them is a copy (or `git subtree split --prefix=staging/skill-gate`). | n/a | **Yes**: approve creating `skill-gate` and a PR adding `workspace/` to `portfolio-brief-gate`. |
| A2 | M1 | PRD §0.1 mentions "the Gemini Gem instructions" in `portfolio-brief-gate`. There is no separate Gem file: the Gem path is a README section telling the user to paste the rubric, watermelon check and judge under SKILL.md's Workflow section. | Read the whole repo at `e852573`. | n/a | No |
| A3 | M1 | Workspace skills cannot run code or make a second model call (PRD §12 Q1–Q2). The in-skill quote check is done by the model; the deterministic check happens afterward, outside Workspace. | PRD §12 says to assume no. | n/a | No |
| A4 | M1 | How reference files are attached and retrieved is unknown (PRD §12 Q3). The skill names each reference file by a Google Doc title, the README gives two setup options (attach, or paste at the end of the skill Doc), and the skill stops with a fixed line if a reference file is missing instead of improvising. | Improvising a rubric would be a silent failure. | Yes | No |
| A5 | M1 | Whether side-panel Gemini reasons before it writes its visible answer is unknown. R1.2 puts the gate result at the top of the output, above the brief it describes. The skill tells the model to finish the self-check first and requires the gate line to match the Self-check record at the bottom; Skill Gate will check that match deterministically. | Makes a wrong top-line result detectable instead of assuming it cannot happen. | Yes | No |
| A6 | M1 | An evidence quote is required for every graded update, not only grades 4–5 as in the original rubric. | R1.3: without a validator inside Workspace, every grade must be checkable afterward. The original example already quoted every grade. | Yes | No |
| A7 | M1 | ABSTAIN counts as not PASS, so any ABSTAIN makes the gate FLAGGED. | The original says the gate is PASSED "only if all four PASS"; made explicit. | Yes | No |
| A8 | M1 | NO-LAUNDER with no CONTRADICTS verdicts, and ACTIONABLE with no flagged program in the body, are PASS with a critique saying there was nothing to check. | The original reserves ABSTAIN for a missing brief. Treating nothing-to-check as ABSTAIN would flag every healthy portfolio. | No: the stricter reading (ABSTAIN, so FLAGGED) would flag correct briefs. | Yes, if you prefer ABSTAIN |
| A9 | M1 | For ACTIONABLE, a "flagged program" is one with a CONTRADICTS or NEUTRAL watermelon verdict, a grade of 3 or higher, or a row downgraded to unverified, counted among programs in the brief body. | The original leaves "flagged" undefined. Its example gave concrete steps to exactly these programs, including the NEUTRAL one. | Yes (wider than CONTRADICTS-only) | No |
| A10 | M1 | If the requester asks to leave out a program, the skill leaves it out and lists it in Coverage; if that program is graded 4–5, COMPLETE fails and the brief is FLAGGED. This is the R1.8 "input that should make the self-check fail" (`examples/ex05`). | Follows from the original COMPLETE definition. It is a realistic governance failure, and no revision can fix it without overriding the requester, so the expected result is unambiguous. | n/a | No |
| A11 | M1 | If the expected number of programs is not given and the requester does not answer the one question, the skill continues and states that missing programs cannot be detected. COMPLETE does not fail for this alone. | The original says "ask once" and does not make a missing count a failure. Failing every brief run from a Studio flow would hide real failures among constant false ones. | No: the stricter reading would fail COMPLETE. | **Yes**: confirm, or choose FAIL |
| A12 | M1 | Duplicates: an identical second copy is excluded as a duplicate; two different updates for one program and period are both graded, reported at the more severe grade, and raised as an Open question; an older period is excluded as superseded. N in "N of M" counts programs, not update messages. | R1.5 requires defined behavior; the original has none. More severe grade is the conservative choice. | Yes | No |
| A13 | M1 | The Workspace edition is versioned 1.0.0 on its own semver line, independent of the original's "v1.0". The version appears in `skill.md`, each reference file header, and the output footer. | R1.6; the footer ties each output to a version. | n/a | No |
| A14 | M1 | The ten examples in `workspace/examples/` are development material: the skill author has seen them. The PBG dogfood run in M4 will use them as the dev split and add separate cases for the holdout split. | PRD §5.3: holdout cases must not be ones the author tuned against. | Yes | No |
| A15 | M1 | Examples reuse the original's fictional "Northwind Group" framing with new program and people names. The worked example reuses the original's W34 portfolio so the few-shot example never overlaps a test case. | PRD §0.3: fictional data only. | n/a | No |
| A16 | M1 | "Tests pass, CI green" in the M1 definition of done: M1 contains no code, and CI is an M3 deliverable. The M1 check is mechanical: every quote in the worked example is verified verbatim with the original `validate_quotes.py`. | PRD §11 puts CI in M3. | n/a | No |
| A17 | M2 | The owner answered the M1 questions with "Go ahead" and no changes, so the M1 defaults stand: work stays staged in `portfolio` (A1), nothing-to-check criteria PASS (A8), and a missing expected count is stated rather than failed (A11). | Recorded so the choice is visible; any of the three can still change. | n/a | No |
| A18 | M2 | Default judge model: `claude-opus-5`, taken from the Claude API model catalog bundled with Claude Code (cached 2026-06-24). Current Claude models have no separate dated ID; the ID is the exact version. Older aliases that point at dated snapshots (for example `claude-haiku-4-5`) are rejected with the dated ID to use. The opt-in live test confirms the served model. Gemini model IDs are verified in M3, when API mode needs them. | PRD §0.5: exact versions only, verified from official sources. | Yes | No |
| A19 | M2 | No server-side refusal fallback on judge calls. A refusal, a truncated answer, malformed JSON or an API failure after retries is ERROR. The model that served each call is recorded; a receipt whose calls were served by a model other than the configured one is NOT VERIFIED. | PRD §5.4: no silent fallback to another model; a receipt must name the model that judged. (The Claude API guidance recommends fallbacks by default; the PRD overrides it here.) | Yes | No |
| A20 | M2 | Retries use the Anthropic SDK's built-in exponential backoff (connection errors, 408, 409, 429, 5xx), with `max_retries` from `skillgate.yaml`, rather than a second retry loop. | Avoids reimplementing SDK behavior; the attempt outcome is still logged. | n/a | No |
| A21 | M2 | Judge "settings" are `effort` and `max_tokens`. Claude Opus 5 rejects `temperature` and `top_p`, so no sampling temperature is recorded for the judge. Thinking runs at the model's adaptive default. | PRD §5.3 asks to record sampling settings; these are the ones that exist for this model. | n/a | No |
| A22 | M2 | The judge's evidence must be a non-empty passage found verbatim in the output (whitespace collapsed, curly quotes straightened). For a property about something missing, the prompt asks the judge to quote where it would have to appear. | PRD §5.3 evidence validation, applied to FAIL verdicts too. | Yes | No |
| A23 | M2 | An empty or missing output file in manual mode is ERROR ("the case was not run"), not FAIL. API mode (M3) will judge a genuinely empty model answer as an output. | An unfilled file is a check that could not be completed. | Yes | No |
| A24 | M2 | Calibration is NOT APPLICABLE when no check used the model judge; such a run can be VERIFIED without calibration. Any model judgment without a calibration record means JUDGE UNCALIBRATED. | Calibration measures the model judge; deterministic checks have nothing to calibrate. | n/a | Yes, if you want calibration required regardless |
| A25 | M2 | VERIFIED requires: at least 10 cases; at least 30% tagged edge or should_not_flag; at least 30% in holdout; every golden case in the run; k of 2 or more; manual capture (surface and date) filled in; split draws that recompute from their seeds. Fewer than 12 cases is a warning only. | PRD §5.3 says "warn" for the 30% edge share but "golden set meets minimums" for VERIFIED; the stricter reading makes the share a blocker. | Yes | No |
| A26 | M2 | Uncovered rules are reported in the receipt but do not block VERIFIED. | PRD §5.3 lists the VERIFIED conditions explicitly, and uncovered rules are not among them. | No | Yes, if you want them to block |
| A27 | M2 | Holdout protection: run reports and receipts show holdout case IDs, status and failing check IDs, never inputs, reasons or evidence. Manual mode writes a separate holdout run sheet, because a person must paste those inputs; ideally not the skill's author. Skill Gate cannot hide files on disk. | PRD §5.3: holdout inputs never shown in dev reports; the manual path needs the inputs somewhere. | Yes | No |
| A28 | M2 | Rules are extracted deterministically: list items, table body rows, and sentences with rule words (must, never, only, reply, exactly, …). A rule key is `k` plus the first 8 hex digits of the SHA-256 of its normalized text, so reordering keeps keys and rewording changes them. Some extracted rows (for example a reference-file table) are not really rules and simply show as uncovered. | Coverage needs rule IDs without a model call. | n/a | No |
| A29 | M2 | Misses and false flags come from expectation kinds: a failed `detect` expectation is a miss, a failed `no_flag` expectation is a false flag, counted if it failed on any repeat. | PRD §5.3 asks for both counts; kinds make them computable. | n/a | No |
| A30 | M2 | Every case needs at least one expectation. An expectation written as quoted expected text is rejected. A criterion using partial-credit words (score, scale, mostly, partially, …) is rejected; one containing "and"/"or" gets a warning. | PRD §5.3: binary statements, never exact expected text; exactly one observable property. | Yes | No |
| A31 | M2 | The data-handling notice is printed before lint's model pass, criteria drafting and model judging, and the first one in a project needs confirmation (stored in `.skillgate/state.json`, git-ignored; `--yes` for scripts). `run --mode api` joins in M3. Manual runs send nothing. | PRD §5.5 names `run`; lint, criteria and judge also send data. | Yes | No |
| A32 | M2 | Added `skillgate split` (not in the PRD) to draw splits for hand-written case files; `interview --import` calls the same code. | Cases written directly in YAML need the same seeded draw. | n/a | No |
| A33 | M2 | Receipts carry a consistency hash, which catches accidental edits but is not a signature. `verify-receipt` goes further: it recomputes results, golden-set statistics, split draws and the verdict from the files in the manifest, and re-renders `receipt.md`. | PRD §10: no cryptographic signing in v1. | n/a | No |
| A34 | M2 | `examples/refund-triage` uses hand-written outputs (`fixture-outputs/`), labeled as such in `capture.yaml` and in the receipt. It is the M2 "receipt from a small hand-made case set", not a model run. | M2 acceptance; there are no API keys in the build environment. | n/a | No |
| A35 | M2 | M2's definition of done says "CI green", but the PRD schedules CI for M3. The 66 tests run locally, and the README quick start was replayed from a fresh copy. | PRD §11. | n/a | No |
| A36 | M2 | Prompt files live in `prompts/` at the repo root and are read from the installed source tree (`pip install -e .`), as the PRD layout requires. A non-editable install would need them packaged; not needed yet. | PRD §8 layout. | n/a | No |
| A37 | M3 | No Gemini model ID or price is set by default. Google's model and pricing pages (ai.google.dev, cloud.google.com docs) were blocked by the build environment's network policy, so no ID could be verified from an official source. `skillgate.yaml.example` leaves a placeholder; `run --mode api` refuses to start without an executor model and a price, and before any paid call asks the Gemini API (`models.get`) whether the model exists, recording the name and version it reports. The model that served each call (`model_version`) is recorded too, and a mismatch makes the receipt NOT VERIFIED. | PRD §0.5: verify model IDs from official docs, never invent them. A runtime check against the API is the strongest verification available here. | Yes | **Yes**: fill in the executor model and its price |
| A38 | M3 | API mode assembles the skill as the Gemini system instruction, followed by each reference file under its name, and sends the case input as the user message (`prompts/executor.v1.md`, hashed into every receipt). This is an emulation: how Workspace attaches reference files is unknown (A4), and every API-mode receipt carries the API EMULATION flag. | PRD §5.3: API mode emulates the skill and must say so. | n/a | No |
| A39 | M3 | The executor cache key covers the skill, every reference file, the case file and its input files, the model ID, every setting, the executor prompt hash and the repeat number. Repeats are separate cache entries, so k runs stay k independent samples. The cache lives in `.skillgate/cache/` (git-ignored). Failed calls are never cached. | PRD §5.4 names skill, input, model and settings; the extra parts make any change that could alter the output miss the cache. | Yes | No |
| A40 | M3 | An API error after the SDK's retries, a blocked prompt, or an answer stopped for safety, recitation or similar is ERROR for that repeat (so the case blocks VERIFIED). An answer cut off at `max_output_tokens` is judged as it is, with its finish reason recorded, because that is what the skill produced under its settings. An empty completed answer is judged, not ERROR (the manual-mode rule in A23 does not apply). | An API failure is a check that could not be completed; a truncated answer is real behavior of the configuration. | Yes | No |
| A41 | M3 | `estimate` counts tokens offline from characters (4 per token by default), assumes fixed output lengths per call, and adds a 25% margin, all configurable. `budget.max_usd` is required for any paid step; `run --mode api` and `judge` each check their own estimate before creating a client, and stop above the budget unless `--confirm-budget`. A model with no price in `skillgate.yaml` cannot be estimated, so the paid step refuses to start. Cached calls count as free. | PRD §5.4: estimate before every run, stop above the cap. A cost that cannot be estimated is treated as over budget. | Yes | No |
| A42 | M3 | The calibration sample is drawn only from dev-case model judgments with a PASS or FAIL verdict (never holdout, never ERROR), at least 30 items, with up to half of them the judge's FAIL verdicts. Both agreements must reach `ceil(0.90 × n)`: overall, and on the judge's FAIL verdicts. A sample with no FAIL verdicts cannot calibrate. | PRD §5.3; holdout details must never reach a person who may tune the skill (A27); ceiling rounding is the stricter reading of "at least 90%". | Yes | No |
| A43 | M3 | A calibration record belongs to one judge identity: model ID, settings (effort, max_tokens) and judge prompt SHA-256. The PRD names model and prompt; settings are added, so changing effort also requires recalibration. | A different effort can change verdicts. | Yes | No |
| A44 | M3 | Blindness is enforced by what the terminal and the exported sheet show (no verdicts, no reasons, no evidence). Skill Gate cannot stop a person from opening the judgment files on disk; the sheet asks them not to. | Files on disk cannot be hidden (same limit as A27). | n/a | No |
| A45 | M3 | `check-stale` compares the latest receipt (by folder name) with the project now. Beyond the PRD's skill, reference, model and judge-prompt changes, it also reports executor settings, the executor prompt, criteria and golden cases. It exits 1 when stale, for CI or a scheduled job. | A receipt describes all of these; any change means it no longer describes the project. | Yes | No |
| A46 | M3 | Run, judgment, receipt and calibration folders are named with UTC time stamps to the microsecond, so two created in the same second never collide and name order equals creation order. | Found while testing: runs created within one second collided, and Skill Gate never overwrites. | n/a | No |
| A47 | M3 | CI lives in two places while staged: `.github/workflows/skill-gate.yml` at the `portfolio` root (runs on changes under `staging/skill-gate/`) and `staging/skill-gate/.github/workflows/ci.yml`, which becomes the new repository's workflow after the move (A1). Both run the tests, `verify-receipt` on every committed receipt, and `check-stale` on the example. | GitHub runs workflows only from the repository root. | n/a | No |
| A48 | M3 | No API keys exist in the build environment, so every Gemini and Claude call in the tests is faked, and API mode has not yet been run against the real Gemini API. The opt-in live test still covers one real judge call. | PRD §0.4: never hardcode keys. | n/a | **Yes**: a first real API-mode run needs `GEMINI_API_KEY` |
| A49 | M3+ | Skill Gate loads API keys from the nearest `.env` (the working directory, then each parent up to the repository root). Variables already set in the shell win, and empty values are skipped. The file is git-ignored, and `.env.example` is the committed template. | The owner found exporting keys by hand tedious. Keys still reach Skill Gate only as environment variables (PRD §0.4), and nothing is printed. | n/a | No |
| A50 | M3+ | Resolves A37. `skillgate.yaml.example` now names `gemini-3.8-flash`, checked on 2026-09-25 on Google's model page (listed there as stable), with its paid-tier price from Google's pricing page ($0.75 input, $3.75 output per million tokens, until 2026-12-31). Gemini 3.1 Pro was not chosen because it exists only as a preview, which Google can retire on two weeks' notice; a receipt should name a model that stays put. Skill Gate still checks the model with the API before any paid call. Google says free-tier requests are used to improve its products, so the README and `.env.example` recommend a paid-tier key for anything that is not fictional. | PRD §0.5: verify model IDs from official docs at build time. | Yes | Recheck the price on 2027-01-01, when it doubles |
| A51 | M3+ | The PRD is not committed. `PRD.md` is git-ignored in this folder because the `portfolio` repository is public; `CLAUDE.md` points to it for local work. | Publishing the owner's planning document is the owner's call. | Yes | Yes, if you want it published |
